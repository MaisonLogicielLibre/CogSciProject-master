language: python
python:
    - 2.7
install:
    - pip install -r requirements.txt
script:
    - nosetests -v --with-coverage --cover-package=bcam_script --cover-inclusive --cover-erase tests
notifications:
    email: false
