# -*- mode: python -*-
a = Analysis(['welcomescript.py'],
             pathex=['/Users/turbosnail9/Desktop/Projects/CogSciProject/bcam_script'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='welcomescript',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='welcomescript')
